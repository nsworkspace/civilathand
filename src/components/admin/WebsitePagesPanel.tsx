"use client";

/**
 * Website Pages & CMS has been retired.
 * Kept as a compatibility stub so older AdminView bundles cannot break
 * during deployment. The module is disconnected from the admin navigation
 * and has no UI or data access.
 */
export function WebsitePagesPanel() {
  return null;
}
